production:
  scheduled_drive_sync:
    class: ScheduledDriveSyncJob
    schedule: every hour at minute 0
    queue: default

development:
  scheduled_drive_sync:
    class: ScheduledDriveSyncJob
    schedule: every hour at minute 0
    queue: default
