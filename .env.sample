# Google OAuth credentials
# To fix the "Missing required parameter: client_id" error:
# 1. Go to https://console.cloud.google.com/
# 2. Create a project or select an existing one
# 3. Go to "APIs & Services" > "Credentials"
# 4. Create an OAuth client ID (or use an existing one)
# 5. Copy the client ID and client secret below
GOOGLE_CLIENT_ID=your_actual_client_id_from_google_cloud_console
GOOGLE_CLIENT_SECRET=your_actual_client_secret_from_google_cloud_console
GOOGLE_API_KEY=your_actual_api_key_from_google_cloud_console

# Mailing List Configuration
# Base domain for your organization (e.g., "example.com")
MAILING_LIST_DOMAIN=example.com
# Subdomain for mailing lists (e.g., "lists")
# This will generate addresses like community@lists.example.com
MAILING_LIST_SUBDOMAIN=lists

# Mailgun Configuration
# Get these from your Mailgun dashboard after adding your domain
MAILGUN_API_KEY=your_mailgun_api_key
MAILGUN_DOMAIN=lists.example.com
MAILGUN_SIGNING_KEY=your_mailgun_signing_key