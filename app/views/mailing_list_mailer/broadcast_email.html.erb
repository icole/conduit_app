<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
      body {
          font-family: Arial, sans-serif;
          line-height: 1.6;
          color: #333;
      }

      .email-header {
          background: #f0f8ff;
          padding: 15px;
          border-left: 4px solid #007cba;
          margin-bottom: 20px;
      }

      .email-content {
          margin: 20px 0;
          white-space: pre-wrap;
      }

      .email-footer {
          margin-top: 30px;
          padding-top: 15px;
          border-top: 1px solid #ddd;
          font-size: 12px;
          color: #666;
      }
  </style>
</head>
<body>
<div class="email-header">
  <strong>Sent by:</strong> <%= @sender.name %> (<%= @sender.email %>)<br>
  <strong>To:</strong> <%= @mailing_list.name %> mailing list
</div>

<div class="email-content">
  <%= simple_format(@message) %>
</div>

<div class="email-footer">
  <p>This message was sent to the <strong><%= @mailing_list.name %></strong> mailing list.</p>
  <p>To reply to all members, send an email to
    <a href="mailto:<%= @mailing_list.email_address %>"><%= @mailing_list.email_address %></a></p>
</div>
</body>
</html>