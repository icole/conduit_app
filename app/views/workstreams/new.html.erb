<% content_for :title, "New Workstream" %>

<div class="max-w-2xl mx-auto">
  <% unless hotwire_native_app? %>
    <h1 class="text-xl sm:text-2xl font-bold mb-4">New workstream</h1>
  <% end %>
  <div class="card bg-base-100 border border-base-200">
    <div class="card-body p-4 sm:p-6">
      <%= render "form", workstream: @workstream %>
    </div>
  </div>
</div>
