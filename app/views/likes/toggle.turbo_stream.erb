<%= turbo_stream.replace dom_id(@likeable, :like_section) do %>
  <% case @likeable %>
  <% when Comment %>
    <%= render "shared/like_button", likeable: @likeable, style: :comment %>
  <% else %>
    <%= render "shared/like_button", likeable: @likeable, style: :default %>
  <% end %>
<% end %>